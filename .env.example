# Vercel Postgres (automatisch ingevuld door Vercel na koppelen van de database)
# Lokaal: kopieer deze waarden uit je Vercel project → Storage → jouw database → .env.local
POSTGRES_PRISMA_URL="postgresql://..."
POSTGRES_URL_NON_POOLING="postgresql://..."
